import 'package:flutter/material.dart';

class DocenteCard extends StatelessWidget{
  const DocenteCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }

}